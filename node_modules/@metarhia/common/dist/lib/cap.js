'use strict';

var capitalize = function capitalize(s) {
  var res = '';
  var prev = true;

  for (var i = 0; i < s.length; i++) {
    var _char = s[i];
    if (prev) res = res + _char.toUpperCase();else res = res + _char.toLowerCase();
    prev = _char === ' ';
    console.log(i, res);
  }

  return res;
};

var str = 'kIeV is a cAPItal of uKrAiNe';
var cap = capitalize(str);
console.log({
  str: str,
  cap: cap
});